/** Search 页占位 — 由 todo39（本地重排 + FTS5 混合检索 RAG v1）接入实装 */
export function SearchPage(): React.JSX.Element {
  return (
    <section className="las-page" aria-labelledby="page-title-search">
      <h1 id="page-title-search" className="las-page-title">
        Search
      </h1>
      <p className="las-page-subtitle">本地 / 云混合搜索</p>
      <div className="las-page-card">
        占位页面 — 由 todo 39（本地重排 + FTS5 混合检索）接线实装。
      </div>
    </section>
  )
}

export default SearchPage
