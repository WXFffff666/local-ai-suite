/** Gallery 页占位 — 由 todo22（parameters 元数据 + 复用预填）接入实装 */
export function GalleryPage(): React.JSX.Element {
  return (
    <section className="las-page" aria-labelledby="page-title-gallery">
      <h1 id="page-title-gallery" className="las-page-title">
        Gallery
      </h1>
      <p className="las-page-subtitle">生图画廊</p>
      <div className="las-page-card">
        占位页面 — 由 todo 22（PNG parameters 元数据 + 复用预填）接线实装。
      </div>
    </section>
  )
}

export default GalleryPage
